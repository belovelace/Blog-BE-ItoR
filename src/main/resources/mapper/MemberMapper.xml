<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blog.member.mapper.MemberMapper">

    <select id="selectByEmail" parameterType="String" resultType="com.blog.member.vo.MemberVo">
        SELECT *
        FROM member
        WHERE email = #{email}
    </select>

    <select id="selectById" parameterType="String" resultType="com.blog.member.vo.MemberVo">
        SELECT * FROM member
        WHERE id = #{id}
    </select>



    <insert id="insertMember" parameterType="com.blog.member.vo.MemberVo">
        INSERT INTO member (
            id, email, pwd, nick, bio, name, birthdate, created_at, is_active
        ) VALUES (
                     #{id}, #{email}, #{pwd}, #{nick}, #{bio}, #{name}, #{birthdate}, now(), #{active}
                 )
    </insert>



</mapper>
